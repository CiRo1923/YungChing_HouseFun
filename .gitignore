# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules
node_modules/.vite

# Logs
logs
*.log

# build / deploy 後由 sync-to-public.mjs 產生的壓縮備份
*.zip

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Claude Code 的個人授權清單:每台機器不同、還混著 scratchpad 的絕對路徑。
# settings.json 只留 hooks(專案共用),permissions 由 .claude/hooks/movePermissionsToLocal.js 搬到這裡。
.claude/settings.local.json
/Backstage/.nuxt
/Backstage/.nuxt
/Official/.nuxt
