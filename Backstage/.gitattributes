# 行尾一律 LF —— 版本庫與工作區都是。
#
# 版本庫裡本來就全部是 LF,差異只出現在工作區:core.autocrlf=true 會在 checkout
# 時轉成 CRLF,但被工具(prettier、各種 mjs 腳本)重寫過的檔案留著 LF,
# 於是同一份檔案在兩個專案的工作區行尾不一致,diff 出來全是假差異。
#
# 這份設定不會改動版本庫內容(本來就是 LF),只讓工作區跟著一致。
* text=auto eol=lf
